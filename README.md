This project uses Render for the backend part of the program and uses GitHub for the frontend. Below is the command to run the code on the terminal, though the API would need to change since it is using the API from render instead of a local host.

GitHub Pages: https://jakub50-coder.github.io/GamblingApp/

Render: https://gamblingapp.onrender.com

Command for local use: .\mvnw.cmd spring-boot:run
